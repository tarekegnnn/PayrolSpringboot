server:
 port: 4343


spring:
 application:
  name: Attendance

 datasource:
  url: jdbc:mysql://localhost:3306/attendances_db?createDatabaseIfNotExist=true
  username: root
  password:
  driver-class-name: com.mysql.cj.jdbc.Driver

 jpa:
  hibernate:
   ddl-auto: update
eureka:
 client:
  serviceUrl:
   defaultZone: http://localhost:8761/eureka
  fetch-registry: true
  register-with-eureka: true
